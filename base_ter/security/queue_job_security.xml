<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="queue_job_batch_user_ter_jobs_rule" model="ir.rule">
        <field name="name">Job Queue batch user: only own batch's jobs</field>
        <field name="model_id" ref="queue_job.model_queue_job"/>
        <field name="groups" eval="[(4, ref('queue_job_batch.group_queue_job_batch_user'))]"/>
        <field name="domain_force">[('job_batch_id.user_id', '=', user.id)]</field>
    </record>

</odoo>
