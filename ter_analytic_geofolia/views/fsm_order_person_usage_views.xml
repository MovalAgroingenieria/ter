<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <!-- Tree view -->
    <record id="fsm_order_person_usage_tree" model="ir.ui.view">
        <field name="name">fsm.order.person.usage.tree</field>
        <field name="model">fsm.order.person.usage</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle" />
                <field name="person_id" optional="show" />
                <field name="name" />
                <field name="hours" widget="float_time" />
                <field name="geofolia_employee_id" optional="hide" />
            </list>
        </field>
    </record>

    <!-- Form view -->
    <record id="fsm_order_person_usage_form" model="ir.ui.view">
        <field name="name">fsm.order.person.usage.form</field>
        <field name="model">fsm.order.person.usage</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="fsm_order_id" />
                            <field name="person_id" />
                            <field name="name" />
                        </group>
                        <group>
                            <field name="hours" widget="float_time" />
                            <field name="geofolia_employee_id" />
                            <field name="geofolia_recognition_id" />
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
