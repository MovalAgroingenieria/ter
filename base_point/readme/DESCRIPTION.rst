Description
===========

Base module for geolocated point-based censuses.

It introduces the point domain models (points, tags, profiles and contact
links), adds point GIS vector configuration in company settings, and provides
the initial menus, views, security groups, ACLs and a wizard to update point
codes.
