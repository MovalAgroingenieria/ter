On module installation, a new product category **Areas** is created with the
appropriate configuration for area-based mass invoicing.

The post-install hook automatically sets the overhead percentage to 100% for
the main stakeholder of each existing parcel.

No additional manual configuration is required.
